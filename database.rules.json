{
  "rules": {
    "counter":{
      ".read": "auth !== null",
          ".write": "auth !== null",
    },
        
    "todo": {
      "$uid": {
        ".read": "true",
        ".write": "auth !== null && auth.uid === $uid",
        ".indexOn":["name","date","cid","deleted"],
      }
    }
  }}
